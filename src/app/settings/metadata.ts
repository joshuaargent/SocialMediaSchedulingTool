import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Settings',
  description: 'Configure your SMST account settings, manage platform connections, and customize your preferences.',
  keywords: ['settings', 'account settings', 'platform connections', 'preferences', 'SMST settings'],
};